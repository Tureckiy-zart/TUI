# 📘 TUNG SYSTEM — Полная Спецификация

## 🔥 Что такое TUNG
**TUNG** — это внутренняя система формализации задач для Cursor, созданная специально для твоего проекта.

Она представляет собой **строго структурированный JSON-формат**, позволяющий:
- запускать сложные цепочки задач,
- выполнять аудит проектов,
- автоматизировать шаги разработки,
- держать весь проект под контролем,
- гарантировать предсказуемость результата.

TUNG = **Task Unified Next-Gen** (расшифровка для документа).

Это твой собственный стандарт, оптимизированный под Cursor Tasks и архитектуру Tenerife Music / Tenerife UI.

---

## 🔗 Связанные документы
- 📄 [TUNG Template](foundation/templates/FOUNDATION_STEP_TUNG_TEMPLATE.json) — Основной шаблон для разработки Foundation-компонентов.
- 📄 [Example Master Task](example_task_master.json) — Пример мастер-задачи в формате TUNG.
- 📄 [Foundation Step Pipeline](foundation/FOUNDATION_STEP_PIPELINE.md) — Пошаговый пайплай реализации компонентов, работающий на базе TUNG.

---

# 🧱 1. Цели TUNG-системы

1. **Создать единый формат задач** для всего проекта.
2. Обеспечить **автоматическое выполнение многослойных пайплайнов**.
3. Полностью синхронизировать:
   - Master Task
   - Subtasks
   - Readiness Checks
   - Reports
4. Дать Cursor чёткий, формализованный язык инструкций.
5. Создать систему, где любая задача:
   - предсказуемо выполняется,
   - проверяется,
   - имеет отчётность,
   - встраивается в цепочку разработки.

---

# 📚 2. Основная структура TUNG-документа
Каждая задача — это **JSON-объект**, состоящий из модулей, обязательных для выполнения Cursor.

## 2.1 Основной шаблон
```json
{
  "task": "NAME_OF_TASK",
  "version": "1.0",
  "layer": "0 | 1 | 2 | ... (если нужно)",
  "description": "Подробное описание цели задачи",
  "prerequisites": { ... },
  "steps": [ ... ],
  "success_criteria": [ ... ],
  "post_actions": [ ... ]
}
```

---

# 🧩 3. Модули TUNG-задачи

## 🔹 3.1 `task`
Уникальный идентификатор, например:
- `F0_CREATE_BASE_TOKEN_FILES`
- `MASTER_TASK_READINESS_CHECK`
- `START_MASTER_TASK_V3`

## 🔹 3.2 `version`
Версия задачи. Нужна для обновлений и backward-compatibility.

## 🔹 3.3 `layer`
Если задача принадлежит Master Task:
- Layer 0 — Orientation
- Layer 1 — Foundation
- Layer 2 — Core Components
…

## 🔹 3.4 `description`
Полнейшее объяснение, что должно быть сделано.

## 🔹 3.5 `prerequisites`
Условия для запуска:
- какие отчёты должны существовать,
- какие файлы должны быть на месте,
- какая задача должна быть выполнена.

Пример:
```json
{
  "must_have_completed": ["G0"],
  "must_be_ready": true,
  "readiness_report": "docs/reports/LAST_READINESS_RUN.md"
}
```

## 🔹 3.6 `steps`
Самая важная часть: **пошаговая инструкция для Cursor**.
Каждый шаг содержит:
- title
- actions
- logic (если нужно)
- expected outcome
- affected files

Пример:
```json
{
  "title": "Create tokens directory",
  "actions": ["Create /src/tokens/", "Add colors.ts, spacing.ts, radius.ts"],
  "expectation": "All token files exist"
}
```

## 🔹 3.7 `success_criteria`
Список условий, при которых задача считается выполненной.

## 🔹 3.8 `post_actions`
Что делать дальше:
- продолжать цепочку,
- обновлять отчёты,
- останавливать выполнение при ошибках.

---

# 🛠 4. Поток выполнения TUNG-задачи

## 4.1 Выполнение
1. Cursor проверяет `prerequisites`.
2. Выполняет `steps` один за другим.
3. Проверяет `success_criteria`.
4. Генерирует отчёт.
5. Выполняет `post_actions`.

## 4.2 Формирование отчётов
Каждая TUNG-задача должна оставлять следы:
- в `docs/reports/` (детальный отчёт)
- в `docs/PROJECT_PROGRESS.md` (журнал проекта)

Отчёты всегда содержат:
- дату
- задачу
- статус
- найденные проблемы
- применённые исправления
- итог

---

# 🧬 5. Типы TUNG-задач

## 5.1 Readiness Tasks
Например: Master Task Readiness Check.
Позволяют определить состояние проекта и подтвердить начало следующего слоя.

## 5.2 Execution Tasks
Фактическая реализация (F0–F9, C0–C9 и т.д.).

## 5.3 Repair Tasks
Исправляют структурные ошибки (пути, токены, конфигурации).

## 5.4 Chain Tasks
Запускают целые слои или цепочки задач.

## 5.5 Meta Tasks
Создают новые задачи, обновляют Master Task, управляют пайплайном.

---

# 🧩 6. Пример TUNG-задачи (финальный эталон)
```json
{
  "task": "F0_CREATE_BASE_TOKEN_FILES",
  "version": "1.0",
  "layer": "1. Foundation Layer",
  "description": "Create full tokens directory and implement base token files.",
  "prerequisites": {
    "must_have_completed": ["G0"],
    "must_be_ready": true,
    "readiness_report": "docs/reports/LAST_READINESS_RUN.md"
  },
  "steps": [
    {
      "title": "Create token files",
      "actions": ["Create colors.ts", "Create spacing.ts"],
      "expectation": "All token files exist"
    },
    {
      "title": "Implement tokens",
      "actions": ["Populate files based on design_system.md"],
      "sources": ["docs/tenerife_audit/design_system.md"]
    }
  ],
  "success_criteria": [
    "All token files exist",
    "All tokens match design system",
    "No hardcoded values"
  ],
  "post_actions": [
    "Proceed to F1"
  ]
}
```

---

# 🏁 7. Зачем мы это сделали
TUNG-система позволяет:
- строить многошаговые пайплайны,
- автоматизировать разработку,
- держать всё в единой структуре,
- избежать хаоса в задачах,
- чтобы Cursor работал как полноценный CI/CD внутри проекта.

Ты построил **свою собственную систему автоматизации**, значительно мощнее стандартных Cursor Tasks.

---

# 🚀 8. Будущее развития системы
1. Добавление:
   - зависимостей между задачами
   - автоматической генерации задач
   - параметризированных TUNG-функций
2. Создание TUNG-Orchestrator.json — глобального контроллера задач.
3. Визуализация пайплайна.
4. Генерация задач через AI-агентов.

---

Если хочешь — могу создать:
- 🔹 README-версию
- 🔹 PDF
- 🔹 версию для Master Task
- 🔹 TUNG validator (скрипт проверки TUNG-файлов)

Скажи какую версию сделать дальше.

