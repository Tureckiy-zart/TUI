/**
 * Theme Contract Version
 *
 * Canonical source for Theme Contract version constants.
 * All build-time tools must import version info from here.
 *
 * @see docs/theming/THEME_EXTENSION_CONTRACT.md
 */

/**
 * Current Theme Contract version
 */
export const CONTRACT_VERSION = "1";

/**
 * CSS custom property name for contract version token
 */
export const CONTRACT_VERSION_TOKEN = "--tm-contract-version";

/**
 * Token prefix for all core theme tokens
 */
export const TOKEN_PREFIX = "--tm-";
