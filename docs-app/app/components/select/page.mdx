---
title: Select
description: Foundation Select (LOCKED). Radix-based, token-driven compound component.
---

import { APITable } from "@/components/docs/APITable";
import { LiveExample } from "@/components/docs/LiveExample";

const SelectRootAPI = {
  name: "SelectRoot",
  description:
    "Radix Select Root (context provider). Controlled/uncontrolled value lives here. Not a DOM element.",
  props: [
    {
      name: "value",
      type: "string",
      required: false,
      description: "Controlled selected value.",
    },
    {
      name: "defaultValue",
      type: "string",
      required: false,
      description: "Uncontrolled initial value.",
    },
    {
      name: "onValueChange",
      type: "(value: string) => void",
      required: false,
      description: "Called when selection changes.",
    },
    {
      name: "disabled",
      type: "boolean",
      required: false,
      description: "Disables the entire Select (delegated to Radix).",
    },
    {
      name: "name",
      type: "string",
      required: false,
      description: "Form field name (Radix passes it to the hidden input).",
    },
    {
      name: "required",
      type: "boolean",
      required: false,
      description: "Marks the Select as required for form submission.",
    },
  ],
} as const;

const SelectTriggerAPI = {
  name: "SelectTrigger",
  description: "Trigger button (combobox). Holds styling tokens and accessible name.",
  props: [
    {
      name: "size",
      type: '"xs" | "sm" | "md" | "lg" | "xl" | Responsive<SelectSizeToken>',
      required: false,
      defaultValue: "md",
      description: "Token-driven size.",
    },
    {
      name: "variant",
      type: '"primary" | "secondary" | "outline" | "ghost" | "destructive"',
      required: false,
      defaultValue: "outline",
      description: "Token-driven visual variant.",
    },
    {
      name: "width",
      type: '"auto" | "full" | "sm" | "md" | "lg" | "xl" | Responsive<SelectWidthToken>',
      required: false,
      defaultValue: "full",
      description: "Token-driven width constraint for the trigger.",
    },
    {
      name: "id",
      type: "string",
      required: false,
      description: "Use with <label htmlFor> for form association.",
    },
    {
      name: "aria-label / aria-labelledby",
      type: "string",
      required: false,
      description: "Required when no visible <label> is associated.",
    },
  ],
} as const;

const SelectContentAPI = {
  name: "SelectContent",
  description: "Dropdown content (portal). Offsets are token-based.",
  props: [
    {
      name: "size",
      type: '"xs" | "sm" | "md" | "lg" | "xl" | Responsive<SelectSizeToken>',
      required: false,
      defaultValue: "md",
      description: "Token-driven size (padding + radius).",
    },
    {
      name: "sideOffset",
      type: "Responsive<SpacingToken>",
      required: false,
      defaultValue: "xs",
      description: "Token-driven offset between trigger and content.",
    },
    {
      name: "alignOffset",
      type: "Responsive<SpacingToken>",
      required: false,
      description: "Token-driven alignment offset for the content.",
    },
    {
      name: "position",
      type: '"popper" | "item-aligned"',
      required: false,
      defaultValue: "popper",
      description: "Radix positioning mode.",
    },
  ],
} as const;

# Select

Foundation Select is a **LOCKED** dropdown selection primitive built on **Radix Select**. It provides a stable compound API and token-driven styling, while delegating accessibility and keyboard behavior to Radix.

## Purpose

- Provide a single canonical dropdown selection primitive for the entire UI system.
- Ensure consistent styling via token unions and consistent behavior via Radix.

## When to use

- Use for single-value selection from a list of options.
- Use inside forms (with a real label) and in UI controls where a listbox/combobox interaction is appropriate.

## When NOT to use

- Do not use for multi-selection (use an Extension that composes Select internally).
- Do not use for free-form input (use `Input`).
- Do not reimplement Select behavior (custom focus/keyboard/ARIA) — Select is the foundation primitive.
- Do not use `LegacySelect` or `selectVariants` (HTML `<select>` legacy implementation). It is backward-compatibility-only and forbidden for new usage.

## Behavioral guarantees

- **Single canonical focus flow**: focus moves to the trigger and returns predictably after selection/close (Radix-managed).
- **Keyboard navigation**: Arrow keys, Enter/Space, Escape behave according to Radix Select semantics.
- **Controlled vs uncontrolled**:
  - Controlled: `value` + `onValueChange`
  - Uncontrolled: `defaultValue`
- **No layout responsibility**: layout is owned by the parent; the component exposes token width on the trigger, not external layout rules.
- **Token-only visuals**: size/variant/width/offsets are token-based. No raw visual values are part of the public API.

## Locked rationale

Select is part of the **Foundation (LOCKED)** layer. Any change to behavior, public exports, or token domains requires the official **UNLOCK** procedure and an audit. The only allowed changes inside the locked component are bug fixes, typing fixes, and documentation/story updates that preserve the public API contract.

## Examples

<LiveExample>
{`import { Select } from "@tenerife.music/ui";

export function Example() {
  return (
    <Select.Root>
      <Select.Trigger aria-label="Select an option">
        <Select.Value placeholder="Select an option" />
        <Select.Icon />
      </Select.Trigger>
      <Select.Content>
        <Select.Viewport>
          <Select.Item value="option1">Option 1</Select.Item>
          <Select.Item value="option2">Option 2</Select.Item>
          <Select.Item value="option3">Option 3</Select.Item>
        </Select.Viewport>
      </Select.Content>
    </Select.Root>
  );
}`}
</LiveExample>

## API

### Root

<APITable data={SelectRootAPI} />

### Trigger

<APITable data={SelectTriggerAPI} />

### Content

<APITable data={SelectContentAPI} />

## Props

`Select` is a compound namespace. Props are defined on subcomponents (`Select.Root`, `Select.Trigger`, `Select.Content`, `Select.Item`, etc.), not on `Select` itself.
