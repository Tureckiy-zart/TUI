import { fontFamily, fontSize, fontWeight, letterSpacing, lineHeight } from "../tokens/typography";

export type FontScale = typeof fontSize;

export {
  fontFamily as fontFamilies,
  fontSize as fontSizes,
  fontWeight as fontWeights,
  letterSpacing as letterSpacings,
  lineHeight as lineHeights,
};
