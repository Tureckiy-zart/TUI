"use client";

/**
 * @deprecated PRIMITIVES/Card is legacy and should not be used in new code.
 *
 * Use one of the following canonical alternatives instead:
 * - COMPOSITION/layout/Card - for general layout card components
 * - PATTERNS/cards/* - for domain-specific card patterns (ArtistCard, VenueCard, etc.)
 *
 * This export will be removed in a future version.
 */

export { Card, CardContent, CardDescription, CardFooter, CardHeader, CardTitle } from "./Card";
